services:
  auth-service:
    build: '.'
    container_name: auth-service
    expose:
      - ${APP_PORT}
    # develop:
    #   watch:
    #     - path: ./src
    #       action: sync
    #       target: /app/src
    #     - path: ./package.json
    #       action: rebuild
